shader_type canvas_item;

uniform vec4 colour = vec4(1,1,1,1);

vec4 overlay(vec4 base, vec4 blend){
	vec4 limit = step(0.5, base);
	return mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), limit);
}

void fragment() {
	COLOR = overlay(COLOR, colour);
}